def handle_background_event(glo, ev):
    print(ev)

"""
        proc_state = None
        proc_run = None

        def proc_set_state(state):
            proc_state = state

        def proc_set_run(run):
            proc_run = run
"""